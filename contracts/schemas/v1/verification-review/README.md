# Verification and Review v1 schemas

Verification and Review's exposed payloads are defined in the shared v1 message
bundle inventoried by [`../catalog.json`](../catalog.json). Contracts bind exact
digests and bounded evidence identities; they do not export process internals or
evidence storage models.
